%td.value= htmlEscape(index.name)
:if index.configAvailable()
  %td.value= htmlEscape(index.provider)
  %td
    %p= htmlEscape(JSON.stringify(index.getConfig()).replace(",",", "))
:if !index.configAvailable()
  %td
    %p N/A  
  %td 
    %p N/A
%td
  %button.delete-index.bad-button Delete
