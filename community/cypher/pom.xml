<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0                       http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.neo4j</groupId>
    <artifactId>parent</artifactId>
    <version>2026.07.0</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>cypher-parent</artifactId>
  <packaging>pom</packaging>
  <name>Neo4j - Community Cypher Build</name>
  <description>Project that builds the Neo4j Cypher modules as part of the Community distribution.</description>
  <url>http://components.neo4j.org/${project.artifactId}/${project.version}</url>

  <modules>
    <module>ir</module>
    <module>cypher-planner</module>
    <module>planner-spi</module>
    <module>planner-spi-test-util</module>
    <module>cypher</module>
    <module>cypher-admin-runtime</module>
    <module>cypher-logical-plans</module>
    <module>front-end</module>
    <module>runtime-util</module>
    <module>interpreted-runtime</module>
    <module>slotted-runtime</module>
    <module>physical-planning</module>
    <module>spec-suite-tools</module>
    <module>logical-plan-builder</module>
    <module>logical-plan-generator</module>
    <module>runtime-spec-suite</module>
    <module>expression-evaluator</module>
    <module>cypher-testing</module>
    <module>cypher-config</module>
    <module>cypher-cache</module>
    <module>graph-counts</module>
    <module>cypher-rendering</module>
    <module>internal-notifications</module>
  </modules>

  <scm>
    <connection>scm:git:git://github.com/neo4j/neo4j.git</connection>
    <developerConnection>scm:git:git@github.com:neo4j/neo4j.git</developerConnection>
    <url>https://github.com/neo4j/neo4j</url>
  </scm>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.reflections</groupId>
        <artifactId>reflections</artifactId>
        <version>0.10.2</version>
        <exclusions>
          <exclusion>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
          </exclusion>
        </exclusions>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>net.alchim31.maven</groupId>
        <artifactId>scala-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>analyze</id>
            <goals>
              <goal>analyze-only</goal>
            </goals>
            <!-- inherit parent config -->
            <configuration>
              <failOnWarning>${dependency.analyze.failOnWarning}</failOnWarning>
              <ignoredUsedUndeclaredDependencies combine.self="override">
                <!-- Override parent POM to NOT ignore org.neo4j:* and com.neo4j:* -->
                <!-- This ensures cypher modules maintain proper dependency declarations -->
                <ignoredUsedUndeclaredDependency>org.apiguardian:apiguardian-api</ignoredUsedUndeclaredDependency>
                <!-- Mixed Scala 2.13 / Scala 3: stdlib is on the classpath transitively; analyzer often reports it as used-undeclared. -->
                <ignoredUsedUndeclaredDependency>org.scala-lang:scala3-library_3</ignoredUsedUndeclaredDependency>
              </ignoredUsedUndeclaredDependencies>
              <ignoredUnusedDeclaredDependencies combine.children="append">
                <!-- pprint exposes a root-level Scala package object — used without an import statement; analyzer cannot detect this pattern. -->
                <ignoredUnusedDeclaredDependency>com.lihaoyi:pprint_${scala.binary.version}</ignoredUnusedDeclaredDependency>
              </ignoredUnusedDeclaredDependencies>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>test-block</id>
      <activation>
        <activeByDefault>false</activeByDefault>
        <property>
          <name>NEO4J_OVERRIDE_STORE_FORMAT</name>
          <value>block</value>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>com.neo4j</groupId>
          <artifactId>neo4j-block-storage-engine</artifactId>
          <version>${project.version}</version>
          <scope>runtime</scope>
        </dependency>
      </dependencies>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.neo4j.build.plugins</groupId>
              <artifactId>licensing-maven-plugin</artifactId>
              <configuration>
                <skip>true</skip>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
  </profiles>
</project>
