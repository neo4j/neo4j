[[query-properties]]
Special properties
==================

Some elements have special properties. Special properties are written using all upper case. If you have a normal
node/relation property named the same as the normal property, escape the property, like this: r.`TYPE`.

include::relationship-type.txt[]
include::path-length.txt[]