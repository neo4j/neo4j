<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.neo4j</groupId>
    <artifactId>parent</artifactId>
    <version>2026.08.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>
  <artifactId>neo4j-import-vector-util</artifactId>

  <packaging>jar</packaging>
  <name>Neo4j - Import Vector Utilities</name>
  <description>Utilities for implementing import functionality with SIMD support.</description>

  <scm>
    <connection>scm:git:git://github.com/neo4j/neo4j.git</connection>
    <developerConnection>scm:git:git@github.com:neo4j/neo4j.git</developerConnection>
    <url>https://github.com/neo4j/neo4j</url>
  </scm>

  <properties>
    <default.jupiter.test.mode>concurrent</default.jupiter.test.mode>
    <jdk25.custom.options>${jdk25.vm.options}</jdk25.custom.options>
    <moduleName>org.neo4j.util.importer.vector</moduleName>
    <test.runner.jvm.settings.additional>--add-modules jdk.incubator.vector</test.runner.jvm.settings.additional>
    <vm.target.version>25</vm.target.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.neo4j</groupId>
      <artifactId>neo4j-import-util</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration combine.self="append">
          <compilerArgs combine.self="append">
            <arg>--add-modules</arg>
            <arg>jdk.incubator.vector</arg>
          </compilerArgs>
          <!-- This module targets JDK 25 (vm.target.version). A graceful per-module toolchain lets
               it be compiled with JDK 25 while the rest of the reactor is built in-process by a
               Maven running on the baseline JDK - so only this module forks. Falls back to the JDK
               running Maven when no matching toolchain is registered (then release 25 simply
               requires Maven itself to run on JDK 25+). -->
          <jdkToolchain>
            <version>${vm.target.version}</version>
          </jdkToolchain>
        </configuration>
      </plugin>
      <!-- Run this module's unit tests on its target JDK too (Maven itself runs on the lower
           baseline JDK, which cannot load this module's newer bytecode). This module has no
           integration tests, so only surefire needs it. Merges with the inherited config. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <jdkToolchain>
            <version>${vm.target.version}</version>
          </jdkToolchain>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
