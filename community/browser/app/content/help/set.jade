article.help
  .container-fluid
    section.row-fluid.summary
      .summary.span3
        h3 SET
        p.lead property updates
      .details.span9
        p
          | The&nbsp;
          code SET
          |  clause updates properties on nodes and relationships.
        table.table-condensed.table-help
          tr
            th Reference:
            td
              code
                a(href='http://docs.neo4j.org/chunked/{{neo4j.version}}/query-set.html') SET
              |  manual page
          tr
            th Related:
            td
              a(help-topic='match') :help MATCH
              | &nbsp;
              a(help-topic='where') :help WHERE
              | &nbsp;
              a(help-topic='cypher') :help Cypher
    section.row-fluid.example
      .span9.offset3
        figure
          pre.code.runnable.
            MATCH (fan:Person)-[w:WATCHED]->(movie)
            WHERE fan.name = "Mikey"
            SET w.rating = 5
          figcaption Mikey likes everything, so give any movie he has watched 5 stars.

