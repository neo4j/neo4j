
# This jar contains web resources (javascript, css, html, images) that
# neo4j-server expects on its classpath.

# Library dependencies
${PACKAGE_NAME}: executable-jar-without-main-class

# Browser
${PACKAGE_NAME}: codeless-jar

# We have wrapper scripts
${PACKAGE_NAME}: missing-dep-on-jarwrapper

# No it's not insecure
${PACKAGE_NAME}: possibly-insecure-handling-of-tmp-files-in-maintainer-script
${PACKAGE_NAME}: possibly-insecure-handling-of-tmp-files-in-maintainer-script
