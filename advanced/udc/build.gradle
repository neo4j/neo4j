dependencies {
  compile project(":kernel"),
          "org.apache.httpcomponents:httpcore:$httpComponentsVersion",
          "commons-io:commons-io:$commonsIoVersion"

  testCompile(group: "org.apache.httpcomponents", name: "httpclient", version: "$httpComponentsVersion", force: true)
  testCompile(group: "org.apache.httpcomponents", name: "httpclient", version: "$httpComponentsVersion", classifier: "tests", force: true)

  // testCompile "org.apache.httpcomponents:httpclient:$httpComponentsVersion",
  // testCompile "org.apache.httpcomponents:httpclient:$httpComponentsVersion:tests"
}

/*
sourceSets {
  test {
    compileClasspath += project(':kernel').sourceSets.test.classes
  }
}                   */
